import { Section } from '@/components/section/Section'
import { Heading } from '@/components/heading/Heading'

export function Hero() {
  return (
    <Section id="hero">
      <Heading level={1} size="display">Hero — coming soon</Heading>
    </Section>
  )
}
